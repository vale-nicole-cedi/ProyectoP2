#include <bits/stdc++.h>
#include "rlutil.h"
#include "usuario.h"
#include "redsocial.h"
using namespace std;
using namespace rlutil;

int main()
{

    cout << "bienvenido a Nonsense" << endl
         << endl;

    cout << "menu:" << endl;
    cout << "da click en el numero indicado para continuar" << endl;

    cout << "1. ver lista de usuarios" << endl; //ascii 49
    cout << "2. ver lista de publicaciones" << endl;
    cout << "3. entrar a un perfil de usuario" << endl;
    cout << "4. agregar un nuevo usuario" << endl;
    cout << "0. salir" << endl;

    while(true){

            if(kbhit()){
            int k = getkey();

            if(k == 49){
                
            }

            if(k == 50){

            }

            if(k == 51){
              
            }

            if(k == 52){

            }

            if(k == 48){
                return 0;
            }

        }
    }
}